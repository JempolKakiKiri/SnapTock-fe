pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,jsx,ts,tsx}"
      run: npx oxlint {staged_files}

    prettier:
      glob: "*.{js,jsx,ts,tsx,json,css,md}"
      run: npx prettier --check {staged_files}